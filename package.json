{
  "name": "xenon",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --scope @xenonapp/xenon --stream",
    "clean": "lerna clean",
    "prepare": "lerna bootstrap",
    "start": "lerna run start --scope @xenonapp/xenon --stream",
    "test": "lerna run test --parallel",
    "watch": "lerna run watch --parallel",
    "watch-chrome": "lerna run watch --parallel"
  },
  "dependencies": {
    "lerna": "^2.8.0"
  }
}
