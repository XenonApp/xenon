{
  "name": "xenon",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "prepare": "lerna bootstrap",
    "start": "lerna run start --scope xenon",
    "test": "lerna run test --parallel"
  },
  "dependencies": {
    "lerna": "^2.8.0"
  }
}
